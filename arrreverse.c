#include<stdio.h>

void main(){
     int arr[i];
     int i,n;

     printf("input the number of element to store in an array: ");
     for(i=0; i<4; i++){

     printf("elements in array\n- %d\n  ",arr[i]);
     scanf("%d",&n);
     }
     printf("element in the array are %d ",arr[i]);

     //reversing an array

      printf("values stored in array are: ");
      for(i=n-1;i>=0;i--){
      printf("%d",a[i]);

      }

     }






